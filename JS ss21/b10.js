if(condition) {
    //logic 1
} else if(condition2) {
    //logic2
} else if(condition3) {
    //logic3
} else{

}
// sử dụng câu lệnh if/else khi cần kiểm tra nhiều điều kiện khác nhau và các điều kiện phức tạp và thực hiện hành động tương ứng

switch (variable) {
    case value1:
        //do something
    break;
    case value2:
        // do somthing else
    break;
    default;
    //do something by default
}
// sử dụng switch/case để so sánh giá trị của biến option với các giá trị được liệt kê trong các case. Mỗi case đại diện cho một giá trị cụ thể và thực hiện hành động tương ứng. Điều default sẽ thực hiện nếu giá trị của biến không khớp với bất kỳ case nào.